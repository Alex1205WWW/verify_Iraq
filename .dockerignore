node_modules
.next
.git
.gitignore

# Local state. The container builds its own on the mounted volume.
.data
prisma/dev.db
prisma/test.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm

# Never bake secrets into an image.
.env
.env.local

test
README.md
