node_modules
.next
out
.env
.env.local
.data
next-env.d.ts
*.tsbuildinfo

# Local SQLite files kept with the archived database design
archive/**/*.db
archive/**/*.db-journal

# Cloudflare local secrets and build output (archived deployment path)
.dev.vars
.wrangler
.open-next
